import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.lounasehdotus.app',
  appName: 'lounasehdotus',
  webDir: 'dist',
  bundledWebRuntime: false
};

export default config;
